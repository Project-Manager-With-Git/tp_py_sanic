from sanic import Sanic, Blueprint
from .{{ source }}_source import {{ source }}


def init(app: Sanic) -> Sanic:
    ws = Blueprint("ws", url_prefix="/ws")
    ws.add_websocket_route({{ source }}, "/{{ source }}")
    app.blueprint(ws)
    return app
